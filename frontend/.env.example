# Backend API Configuration
# Development: Vite proxies /api to this URL
# Production: Frontend connects directly to this URL
# Leave empty to use relative /api path (same origin)
VITE_API_BASE_URL=http://localhost:8015
